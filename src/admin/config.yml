backend:
  name: github
  repo: BigDumbYak-Git/theatre17website
  branch: main
  base_url: https://theatre17-cms-auth.bigdumbyak.workers.dev

media_folder: "src/assets/uploads"
public_folder: "/assets/uploads"

site_url: https://theatre17website.bigdumbyak.workers.dev

# =============================================
# COLLECTIONS
# Each collection = one content type in the CMS
# =============================================

collections:

  # -------------------------------------------
  # SHOWS
  # Used for both the current show and the archive.
  # Editors fill in all fields; the site uses
  # the "current_show" flag to determine what
  # appears on the homepage and Current Show page.
  # -------------------------------------------
  - name: "shows"
    label: "Shows"
    label_singular: "Show"
    folder: "src/shows"
    create: true
    slug: "{{year}}-{{slug}}"
    summary: "{{title}} ({{year}}) — {{status}}"
    preview_path: "show.html"
    fields:

      - label: "Show Title"
        name: "title"
        widget: "string"
        hint: "e.g. The Importance of Being Earnest"

      - label: "Playwright / Writer"
        name: "playwright"
        widget: "string"
        hint: "e.g. Oscar Wilde, Rodgers & Hammerstein"
        required: false

      - label: "Season Year"
        name: "year"
        widget: "number"
        value_type: "int"
        min: 1972
        max: 2100
        hint: "The year the show ran"

      - label: "Season"
        name: "season"
        widget: "select"
        options: ["Spring", "Fall", "Winter", "Summer", "Special"]
        hint: "Which season of the year did this run?"

      - label: "Show Status"
        name: "status"
        widget: "select"
        options:
          - { label: "Planning", value: "planning" }
          - { label: "Auditions Open", value: "auditions" }
          - { label: "In Rehearsal", value: "rehearsal" }
          - { label: "Performances Underway", value: "performances" }
          - { label: "Closed", value: "closed" }
        default: "closed"

      - label: "This is the current show"
        name: "current_show"
        widget: "boolean"
        default: false
        hint: "Turn ON for the show currently running or upcoming. Turn OFF for all past shows. Only one show should have this set to ON at a time."

      - label: "Performance Dates"
        name: "dates"
        widget: "string"
        hint: "e.g. April 25 – May 4, 2025"
        required: false

      - label: "Opening Night"
        name: "opening_date"
        widget: "datetime"
        format: "YYYY-MM-DD"
        date_format: "MMMM D, YYYY"
        time_format: false
        required: false

      - label: "Closing Night"
        name: "closing_date"
        widget: "datetime"
        format: "YYYY-MM-DD"
        date_format: "MMMM D, YYYY"
        time_format: false
        required: false

      - label: "Show Times"
        name: "show_times"
        widget: "string"
        hint: "e.g. Fridays & Saturdays 7:30 PM · Sunday 2:00 PM"
        required: false

      - label: "Venue"
        name: "venue"
        widget: "string"
        default: "The Centre"
        required: false

      - label: "Show Poster"
        name: "poster"
        widget: "image"
        required: false
        hint: "Upload the show poster. Ideal ratio is 2:3 (portrait). JPG or PNG."

      - label: "Show Synopsis"
        name: "description"
        widget: "markdown"
        hint: "A few paragraphs describing the show for the public. This appears on the show page and the homepage."
        required: false

      - label: "Director's Note"
        name: "directors_note"
        widget: "markdown"
        hint: "Optional message from the director, displayed on the show page."
        required: false

      - label: "Ticket / Box Office Link"
        name: "ticket_url"
        widget: "string"
        hint: "Full URL to ticket purchase page (e.g. https://showstix4u.com/...)"
        required: false

      # Cast list — a repeating list of name + role pairs
      - label: "Cast"
        name: "cast"
        widget: "list"
        required: false
        hint: "Add each cast member. Click + to add a row."
        fields:
          - label: "Actor Name"
            name: "name"
            widget: "string"
          - label: "Role / Character"
            name: "role"
            widget: "string"

      # Creative/crew list — same structure as cast
      - label: "Creative & Production Team"
        name: "crew"
        widget: "list"
        required: false
        hint: "Add each crew or creative team member."
        fields:
          - label: "Name"
            name: "name"
            widget: "string"
          - label: "Role / Position"
            name: "role"
            widget: "string"
            hint: "e.g. Director, Set Design, Stage Manager, Lighting"

      # Photo gallery — multiple image uploads
      - label: "Production Photos"
        name: "gallery"
        widget: "list"
        required: false
        hint: "Upload production photos one at a time. They will appear in the gallery on the show page."
        fields:
          - label: "Photo"
            name: "image"
            widget: "image"
          - label: "Caption"
            name: "caption"
            widget: "string"
            required: false
            hint: "Optional photo caption"

  # -------------------------------------------
  # MEMBER BIOS
  # One entry per person. Can be linked from
  # show cast/crew lists by name.
  # -------------------------------------------
  - name: "members"
    label: "Members & Bios"
    label_singular: "Member"
    folder: "src/members"
    create: true
    slug: "{{slug}}"
    summary: "{{name}} — {{primary_role}}"
    fields:

      - label: "Full Name"
        name: "name"
        widget: "string"

      - label: "Photo"
        name: "photo"
        widget: "image"
        required: false
        hint: "Headshot or portrait. Square or portrait orientation works best."

      - label: "Primary Role(s)"
        name: "primary_role"
        widget: "string"
        hint: "e.g. Actor, Director, Set Designer, Stage Manager"

      - label: "Member Type"
        name: "member_type"
        widget: "select"
        options:
          - { label: "Current Member", value: "current" }
          - { label: "Alumni", value: "alumni" }
        default: "current"

      - label: "Performer or Crew?"
        name: "performer_or_crew"
        widget: "select"
        options:
          - { label: "Performer (on stage)", value: "cast" }
          - { label: "Crew / Creative (backstage)", value: "crew" }
          - { label: "Both", value: "both" }
        default: "cast"

      - label: "Bio"
        name: "bio"
        widget: "markdown"
        hint: "A short biography. 2–4 sentences works well."

      - label: "Shows (for display)"
        name: "shows_display"
        widget: "string"
        required: false
        hint: "A brief comma-separated list of notable shows, e.g. Earnest · Steel Magnolias · Wizard of Oz"

      - label: "Personal Website or Link"
        name: "external_url"
        widget: "string"
        required: false
        hint: "Optional — a personal website, LinkedIn, etc."

  # -------------------------------------------
  # NEWS POSTS
  # Blog-style entries for announcements,
  # audition calls, awards, community notices.
  # -------------------------------------------
  - name: "news"
    label: "News & Announcements"
    label_singular: "Post"
    folder: "src/news"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    summary: "{{title}} ({{date}})"
    sortable_fields: ["date", "title", "category"]
    view_filters:
      - label: "Auditions"
        field: "category"
        pattern: "auditions"
      - label: "News"
        field: "category"
        pattern: "news"
      - label: "Awards"
        field: "category"
        pattern: "awards"
    fields:

      - label: "Post Title"
        name: "title"
        widget: "string"

      - label: "Date"
        name: "date"
        widget: "datetime"
        format: "YYYY-MM-DD"
        date_format: "MMMM D, YYYY"
        time_format: false
        default: ""

      - label: "Category"
        name: "category"
        widget: "select"
        options:
          - { label: "News", value: "news" }
          - { label: "Auditions", value: "auditions" }
          - { label: "Awards", value: "awards" }
          - { label: "Community", value: "community" }
          - { label: "Youth Theatre", value: "youth" }
        default: "news"

      - label: "Featured Image"
        name: "image"
        widget: "image"
        required: false
        hint: "Optional photo to accompany the post."

      - label: "Summary"
        name: "summary"
        widget: "text"
        hint: "1–2 sentences shown in the news listing. Keep it brief."
        required: false

      - label: "Full Post Body"
        name: "body"
        widget: "markdown"
        hint: "The full text of the announcement or news item."

  # -------------------------------------------
  # PAGES
  # Single-file collections for editable static
  # pages like About. Each file entry maps to a
  # JSON data file consumed by the page template.
  # -------------------------------------------
  - name: "pages"
    label: "Pages"
    files:

      - label: "About Page"
        name: "about"
        file: "src/_data/about.json"
        fields:

          - label: "Page Header"
            name: "page_header"
            widget: "object"
            fields:
              - label: "Kicker (small label above heading)"
                name: "kicker"
                widget: "string"
                hint: "e.g. Who We Are"
              - label: "Heading"
                name: "heading"
                widget: "string"
              - label: "Subheading"
                name: "subheading"
                widget: "string"

          - label: "Content Sections"
            name: "content_sections"
            widget: "list"
            label_singular: "Section"
            hint: "Main body sections. Add, remove, or reorder freely."
            fields:
              - label: "Heading"
                name: "heading"
                widget: "string"
              - label: "Body"
                name: "body"
                widget: "markdown"
                hint: "Supports bold, italic, links, and lists."

          - label: "Sidebar Cards"
            name: "sidebar_cards"
            widget: "list"
            label_singular: "Card"
            hint: "Cards in the right sidebar. Add, remove, or reorder freely."
            fields:
              - label: "Card Type"
                name: "card_type"
                widget: "select"
                options:
                  - { label: "List Card (label/value rows)", value: "list" }
                  - { label: "Call to Action (body text + button)", value: "cta" }
                default: "list"
              - label: "Heading"
                name: "heading"
                widget: "string"
              - label: "List Items"
                name: "items"
                widget: "list"
                label_singular: "Item"
                required: false
                hint: "Used when Card Type is List. Each item shows as a label/value row."
                fields:
                  - label: "Label"
                    name: "label"
                    widget: "string"
                  - label: "Value"
                    name: "value"
                    widget: "string"
              - label: "Footnote"
                name: "footnote"
                widget: "string"
                required: false
                hint: "Small italic text shown below the list. Leave blank to hide."
              - label: "Body Text"
                name: "body"
                widget: "text"
                required: false
                hint: "Used when Card Type is Call to Action."
              - label: "Button Label"
                name: "button_label"
                widget: "string"
                required: false
                hint: "Used when Card Type is Call to Action. Leave blank for no button."
              - label: "Button URL"
                name: "button_url"
                widget: "string"
                required: false
                hint: "e.g. /contact.html"
              - label: "Highlighted Style (burgundy background)"
                name: "highlighted"
                widget: "boolean"
                default: false
                hint: "Turn on for the dark red accent card style."

  # -------------------------------------------
  # SITE SETTINGS
  # Single-instance file — controls site-wide
  # values like group name, contact info, socials.
  # Only admins should edit this.
  # -------------------------------------------
  - name: "settings"
    label: "Site Settings"
    files:
      - label: "General Settings"
        name: "general"
        file: "src/_data/settings/general.json"
        fields:

          - label: "Group Name"
            name: "group_name"
            widget: "string"
            default: "Theatre 17"

          - label: "Tagline"
            name: "tagline"
            widget: "string"
            default: "Dryden's Community Theatre Since 1972"
            hint: "Short description shown in the footer and meta tags."

          - label: "Contact Email"
            name: "contact_email"
            widget: "string"
            hint: "The main contact email address shown on the Contact page."

          - label: "Facebook Page URL"
            name: "facebook_url"
            widget: "string"
            required: false
            hint: "Full URL to your Facebook page."

          - label: "Instagram URL"
            name: "instagram_url"
            widget: "string"
            required: false

          - label: "Footer Text"
            name: "footer_text"
            widget: "string"
            default: "Dryden's community theatre group, bringing live performance to Northwestern Ontario since 1972."
            hint: "Short tagline that appears under the logo in the footer."

          - label: "City & Province"
            name: "location"
            widget: "string"
            default: "Dryden, Ontario, Canada"
